name: nascente_brasil
version: '1.0.0'
config-version: 2
profile: nascente_brasil
model-paths: [models]
test-paths: [tests]
target-path: target
clean-targets: [target, dbt_packages]
models:
  nascente_brasil:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
